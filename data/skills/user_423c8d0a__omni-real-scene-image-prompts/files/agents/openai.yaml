interface:
  display_name: "全域真实场景万能生图提示词引擎"
  short_description: "面向中国优先、全行业、全平台、全比例的真实场景与自媒体视觉提示词引擎"
  default_prompt: "使用 $omni-real-scene-image-prompts 把任意主题、行业、人物、产品、空间、事件或内容目标，裂变并编排为可直接用于文生图、图生图和局部编辑的高真实度提示词；默认只输出提示词，不直接生成图片。"

policy:
  allow_implicit_invocation: true
