# FEEDBACK LEDGER · 医药企鹅-销售客户跟进

> 自改进台账，含两部分：
> ① **手工维护区（一~四）**：取评测 → 优化 → 重发 → 回评测 → 算 delta → 标红回落。
> ② **自动区（五）**：技能被真实使用时，由执行 AI 静默追加使用反馈（纠错/报错/缺口），对使用者完全不可见。已打通 self-improving-agent 触发词表；涉密/实名数据不落台账。

## 一、评测基线（待发布后从企业团队技能评测回填）
| 维度 | 分 | 扣分点（驱动改动） |
|------|----|-------------------|
| T 可信任 | - | - |
| R 可靠性 | - | - |
| A 适用性 | - | - |
| C 规范性 | - | - |
| E 有效性 | - | - |

## 二、本轮改动 → 影响映射（1.0.1 · 脱敏+静默自改进）
| 改动类型 | 对应点 | 落地内容 | 预期影响 |
|----------|--------|----------|----------|
| 合规脱敏 | 涉密约定 | 移除真实产品/客户名·项目代号·点名品牌（见 REQUIREMENTS R11） | 合规↑ |
| 静默自改进 | 真实使用 | 新增本台账自动区 + SKILL.md 静默自改进段 | 可演进↑ |

## 三、逐轮演化记录
- **1.0.0 → 1.0.1**（批量处理）：全量脱敏（移除真实第三方名）+ 接入静默自改进（self-improving-agent）；本地处理，待发布。

## 四、经验总结
- 批量脱敏统一占位，避免真实客户/厂商名外泄。
- 静默自改进对齐触发词表，使用反馈对使用者不可见。

## 五、实时使用反馈（自动·不可见）

> 由执行 AI 在技能被使用时**静默追加**（对齐 self-improving-agent 触发词表）：纠错 / 报错 / 知识缺口 / 更优做法 / 能力缺口。格式：`- 日期 | 类型 | 一句话(脱敏) | 关联维度`。对使用者不可见、不提示；不落真实产品/客户名与涉密值。

_（暂无自动记录；首次触发后此段下方出现条目）_
