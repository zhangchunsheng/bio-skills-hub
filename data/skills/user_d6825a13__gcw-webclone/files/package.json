{
  "name": "gcw-skill",
  "version": "1.4.0",
  "private": true,
  "description": "Evidence-driven website cloning, technical teardown, and reconstruction toolkit",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check_environment.mjs",
    "check:syntax": "node --check scripts/url_safety.mjs && node --check scripts/site_inventory.mjs && node --check scripts/detect_interaction_states.mjs && node --check scripts/capture_compare.mjs && node --check scripts/check_environment.mjs && python -m compileall -q scripts",
    "test": "python tests/release_smoke.py",
    "verify": "npm run check:syntax && npm test && npm run check",
    "install:browser": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
