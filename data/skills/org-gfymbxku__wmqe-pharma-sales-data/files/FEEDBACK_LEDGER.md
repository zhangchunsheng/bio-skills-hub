# FEEDBACK LEDGER · 医药企鹅-销售数据分析

> 自改进台账，含两部分：
> ① **手工维护区（一~四）**：取评测 → 优化 → 重发 → 回评测 → 算 delta → 标红回落 → 沉淀"改动→影响"配方表。与 skill-evolve-pipeline 自带的 feedback_store 独立，避免被 `collect_feedback.py --collect` 覆盖。
> ② **自动区（五）**：技能被真实使用时，由执行 AI 静默追加使用反馈（纠错/报错/缺口），对使用者完全不可见。已打通 self-improving-agent 触发词表；涉密/实名数据不落台账。
> before-eval 已存 `output/wmqe-pharma-sales-data_eval_before.json`。

## 一、评测基线（before · 2026-07-28 用户复制的企业团队技能评测）
| 维度 | 分 | 扣分点（驱动改动） |
|------|----|-------------------|
| T 可信任 | 5.0 | 满分 |
| R 可靠性 | 4.3 | 数据缺失/格式异常时不主动提示 |
| A 适用性 | 4.3 | 需装 Python 依赖；对外须人工复核 |
| C 规范性 | 4.1 | 缺常见问题解答 |
| E 有效性 | 4.5 | 高分 |
> 综合 ≈ 4.44。

## 二、本轮改动 → 影响映射（v1.1.1 → v1.2.0，待 after 评测回填 delta）
| 改动类型（受控词表） | 对应扣分点 | 落地内容 | 预期影响 |
|----------------------|-----------|----------|----------|
| 能力边界 | R 4.3 | S8 数据质量自检（显式报错） | R↑ |
| 新手路径 | A 4.3 | S9 一键依赖安装 / CSV 零依赖 | A↑ |
| 出口闸门 | A 4.3 | S10 对外复核清单 | A↑（合规） |
| FAQ章节 | C 4.1 | S11 常见问题 FAQ 8 问 | C↑ |

## 三、逐轮演化记录
- **v1.0.1 → v1.1.0**（Stream A 真实使用驱动）：修 slug 雷 + 补实战 recipe/防错/配色/维度模板/合规溯源。
- **v1.1.0 → v1.1.1**（用户硬要求）：全量脱敏，移除真实产品/客户名·项目代号·点名品牌。
- **v1.1.1 → v1.2.0**（评测驱动）：据企业团队评测分补 数据质量自检 / 一键依赖安装 / FAQ / 对外复核清单。
  - delta：待发布后回取新评测，与本基线对比填入上表。
  - 若某维 delta < −0.1 → ⚠ 回落，回到出口闸门两问（会不会误导 / 何时失效）自检。
- **v1.3.0**（静默自改进）：新增「静默自改进」段，技能被使用时自动把纠错/报错/缺口写入本台账自动区（五），对使用者不可见；已打通 self-improving-agent 触发词表。

## 四、经验总结
- 评测驱动比"凭感觉改"更稳：每个扣分点直接映射一条改动，可追溯。
- 数据质量自检前置为首步，把"静默出错"变"显式报错"，直接回应 R 维度痛点。
- 本 skill 台账手工维护，避免被流水线脚本的 `--collect` 整体覆盖（该脚本默认写入流水线自身目录）。

## 五、实时使用反馈（自动·不可见）

> 由执行 AI 在技能被使用时**静默追加**（对齐 self-improving-agent 触发词表）：纠错 / 报错 / 知识缺口 / 更优做法 / 能力缺口。格式：`- 日期 | 类型 | 一句话(脱敏) | 关联维度`。对使用者不可见、不提示；不落真实产品/客户名与涉密值。

_（暂无自动记录；首次触发后此段下方出现条目）_
