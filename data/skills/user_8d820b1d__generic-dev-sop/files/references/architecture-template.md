# <项目名> 架构与产品决策

> 决策日期：<YYYY-MM-DD>
> 仓库：<路径；多仓分别列出>

## 目录

1. [输入与现状](#输入与现状)
2. [产品边界](#产品边界)
3. [总体架构](#总体架构)
4. [客户端架构](#客户端架构)（如适用）
5. [服务端架构](#服务端架构)（如适用）
6. [数据库决策与核心模型](#数据库决策与核心模型)
7. [接口与实时事件](#接口与实时事件)
8. [安全隐私](#安全隐私)
9. [可行性风险与决策门](#可行性风险与决策门)

## 输入与现状

### 产品输入

- <需求来源：共享文档、设计稿、UI Mock、竞品基线等>

### 仓库基线

- <当前仓库是正式工程还是 Mock；哪些数据/功能是假的；第一个阶段必须先做什么>

## 产品边界

### 产品定义

<核心价值一句话 + 核心闭环文字图：入口 → 主流程 → 完成 → 恢复。>

### P0 / P1 / P2

- P0：<首个可发布 MVP 的功能清单>
- P1：<验证留存后加入>
- P2：<有数据/合作方后再做>

### 明确不做

- <通用/扩展能力先排除，防止范围蔓延>

## 总体架构

<文字图：客户端 → 服务端 → 存储/CDN 的数据流；注明首版保持单体、单一主库。>

## 客户端架构（如适用）

### 固定技术栈

- <框架、状态管理、路由、HTTP、媒体/后台、存储、安全存储；沿用团队已验证组合，注明哪些需真机验证>
- <未指定技术栈时，默认采用 `references/default-stack.md` 中「跨端移动应用（Flutter）」的组合>

### 目录边界与约束

<目录骨架 + 约束：view 不直接调平台 SDK、state 拥有加载/错误/权限、data 负责序列化、platform 只输出稳定事件。>

## 服务端架构（如适用）

### 固定技术栈

- <框架、ORM、数据库驱动、迁移、缓存、鉴权、日志、实时事件>
- <未指定技术栈时，默认采用 `references/default-stack.md` 中「服务端（NestJS）」的组合>

### 模块边界

<按业务域拆模块：common/、config/、database/、modules/<业务>/；禁止空泛 repository/factory 层。>

### 横切约定

- API 前缀、全局校验（白名单/拒绝未知字段）、全局异常过滤器。
- 每个请求 requestId，写入响应头与日志。
- Swagger/OpenAPI 作为契约证据；破坏性变更走新版本或兼容窗口。
- 日志 JSON 格式并脱敏。

### 统一错误格式

```json
{
  "code": "<稳定错误码>",
  "message": "<用户可读信息>",
  "details": null,
  "requestId": "...",
  "timestamp": "2026-01-01T00:00:00.000Z"
}
```

### 鉴权与 RBAC

- 公共资源匿名可读，写操作受保护；基础角色清单。
- access 短期 + refresh 轮换/吊销；令牌进系统安全存储；实时握手复用鉴权。

## 数据库决策与核心模型

### 主库选择与理由

- <为什么选它：关系/事务/空间/一致性需求；明确淘汰的候选与理由>

### 缓存与实时边界

- 允许：<缓存、限流、锁、短期会话、实时扇出>
- 禁止：<唯一业务真相、账本余额、归属状态只放缓存>

### P0 核心表/模型

| 表/模型 | 关键字段/约束 | 用途 |
| --- | --- | --- |
| <名称> | <主键、唯一约束、外键、空间列等> | <用途> |

- 空间列建 GiST 索引；高频字段建组合索引；所有账本/幂等写入有唯一幂等键。

### 延后表

<仅在 P1/P2 获批时加入。>

## 接口与实时事件

### P0 REST 资源

- <列出资源与方法；nearby/搜索类注明必须携带的校验参数>

### P0 实时事件

- <仅保留确有实时价值的事件；高频流、位置流、播放器进度默认不走实时通道>
- 断线后通过 REST/本地状态恢复，不依赖事件必达。

## 安全隐私

- 权限分阶段请求，提供拒绝/降级路径。
- 最小化收集；敏感数据默认不上云；诊断数据单独同意、短期保存、可删除。
- 日志与错误上报不包含令牌、密钥、手机号、精确位置或未发布内容。

## 可行性风险与决策门

| 风险 | 结论 | J0 必须验证的门槛 |
| --- | --- | --- |
| <高风险能力> | <结论> | <可判定的真机/测试门槛> |
