# 运行检查清单

- 类别：`数据分析`
- 在执行操作前，先确认用户目标、必需输入和输出格式。
- 当缺少必需输入时，提出有针对性的澄清问题。
- 将回复限定在文档化的工作流范围内，并明确说明假设。
- 在执行任何依赖文件或数据的命令前，先运行一次非破坏性的最小验证。
- 推荐的最小验证命令：`python -m py_compile scripts/main.py`
- 如果执行失败，停止并给出简要的恢复路径，而不是编造结果。
