# AI拟人化写作助手依赖
# 纯Python标准库 + 可选扩展

# 核心依赖（Python 3.8+）
# 无需额外安装，所有核心功能基于标准库实现

# 可选扩展（启用特定功能时安装）
# numpy>=1.24.0    # 向量相似度计算（指纹匹配增强）
# jieba>=0.42.1    # 中文分词（用词偏好精准提取）
# scikit-learn>=1.3.0  # 文本聚类（场景自动检测增强）
