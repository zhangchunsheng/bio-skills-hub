/* ============================================================
   极简风模板 · minimalist v1.0 —— 共享样式
   签名手法：窄容器 / 大留白 / 发丝线 / 三档字重 / 强调色极省
   页面外壳恒为浅底深字（可读性红线）；被采集页的真实底色
   只以色块形式陈列，不用来渲染整张卡。
   强调色由每张卡的 --card-accent 注入，便于多卡共用同一份 CSS。
   ============================================================ */
* { box-sizing: border-box; }
html { -webkit-text-size-adjust: 100%; }
body {
  margin: 0;
  background: #fbfbfa;
  color: #1a1a1a;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
               "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  font-size: 15px;
  line-height: 1.75;
  -webkit-font-smoothing: antialiased;
}
.wrap { max-width: 720px; margin: 0 auto; padding: 56px 24px 96px; }

/* —— 全大写小字距标签：极简风的节奏标记 —— */
.label {
  font-size: 11px;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: #9a9a94;
  font-weight: 500;
  margin: 0 0 14px;
}
.rule { height: 1px; background: #e8e8e4; border: 0; margin: 0; }

/* —— 头部 —— */
.masthead { padding-bottom: 28px; }
.site {
  font-size: 12px; letter-spacing: .06em; text-transform: uppercase;
  color: var(--card-accent, #8a8a84);
  font-weight: 600; margin: 0 0 10px;
}
h1 {
  font-size: 30px; line-height: 1.3; font-weight: 600;
  letter-spacing: -0.015em; margin: 0 0 12px; color: #111;
}
.dek { font-size: 15px; color: #6b6b66; margin: 0 0 18px; }
.meta {
  font-size: 12px; color: #9a9a94; display: flex; flex-wrap: wrap;
  gap: 6px 14px; font-variant-numeric: tabular-nums;
}
.meta a { color: #6b6b66; text-decoration: none; border-bottom: 1px solid #ddd; }
.src { word-break: break-all; }

/* —— 区块 —— */
section { padding-top: 40px; }
section > .rule { margin-bottom: 28px; }

/* —— 配色卡 —— */
.swatches { display: grid; grid-template-columns: repeat(auto-fill, minmax(104px, 1fr)); gap: 12px; }
.sw { border: 1px solid #ecece8; border-radius: 3px; overflow: hidden; background: #fff; }
.sw .chip { height: 52px; }
.sw .info { padding: 8px 10px; border-top: 1px solid #f0f0ec; }
.sw .role { font-size: 11px; color: #9a9a94; letter-spacing: .04em; }
.sw .hex {
  font-size: 12px; color: #333; font-variant-numeric: tabular-nums;
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
}
.sw .usage { font-size: 10px; color: #b4b4ae; margin-top: 2px; }

/* —— 字体 —— */
.type-row { display: flex; gap: 20px; flex-wrap: wrap; }
.type-col { flex: 1 1 260px; min-width: 0; }
.type-col .fam {
  font-size: 14px; color: #333; word-break: break-word;
  padding: 10px 12px; background: #f6f6f3; border-radius: 3px;
  border-left: 2px solid var(--card-accent, #666);
}
.type-col .fam .kind { color: #9a9a94; font-size: 11px; letter-spacing: .06em; }
.specimen { font-size: 24px; margin: 12px 0 0; color: #222; letter-spacing: -0.01em; }

/* —— 节奏参数 —— */
.params { display: flex; flex-wrap: wrap; border-top: 1px solid #ecece8; }
.param { flex: 1 1 33%; min-width: 120px; padding: 16px 0; border-bottom: 1px solid #ecece8; }
.param .k { font-size: 11px; color: #9a9a94; letter-spacing: .06em; text-transform: uppercase; }
.param .v { font-size: 19px; font-variant-numeric: tabular-nums; color: #222; margin-top: 4px; }
.param .v small { font-size: 12px; color: #9a9a94; }

/* —— 风格评分 —— */
.scores { display: grid; gap: 10px; }
.score { display: grid; grid-template-columns: 74px 1fr 46px; align-items: center; gap: 12px; font-size: 13px; }
.score .bar { height: 5px; background: #ecece8; border-radius: 3px; overflow: hidden; }
.score .bar i { display: block; height: 100%; background: #c9c9c2; border-radius: 3px; }
.score.win { color: #111; font-weight: 600; }
.score.win .bar i { background: var(--card-accent, #666); }
.score .pct { text-align: right; color: #9a9a94; font-variant-numeric: tabular-nums; font-size: 12px; }
.note { font-size: 12px; color: #9a9a94; margin: 14px 0 0; padding-left: 12px; border-left: 2px solid #e0e0da; }

/* —— 内容 —— */
.lead { font-size: 16px; color: #2a2a2a; }
blockquote {
  margin: 20px 0; padding: 2px 0 2px 18px; border-left: 2px solid var(--card-accent, #666);
  color: #4a4a46; font-size: 15px;
}
.nums { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 18px; }
.num {
  font-size: 12px; padding: 4px 10px; border: 1px solid #e0e0da;
  color: #444; border-radius: 3px; font-variant-numeric: tabular-nums;
}
.outline { list-style: none; padding: 0; margin: 0; }
.outline li {
  font-size: 14px; color: #3a3a36; padding: 9px 0;
  border-bottom: 1px solid #f0f0ec; display: flex; gap: 10px;
}
.outline li span { color: #c0c0b8; font-variant-numeric: tabular-nums; font-size: 12px; min-width: 18px; }

/* —— 页脚 —— */
footer { padding-top: 40px; font-size: 11px; color: #b4b4ae; letter-spacing: .05em; }
@media print { body { background: #fff; } .wrap { padding: 0; } }

/* —— 体检清单 —— */
.checks { list-style: none; padding: 0; margin: 0; }
.checks li { display: flex; gap: 10px; padding: 9px 0; border-bottom: 1px solid #f2f2ee; font-size: 13px; color: #3a3a36; }
.checks .dot { flex: 0 0 6px; width: 6px; height: 6px; border-radius: 50%; margin-top: 8px; background: #c9c9c2; }
.checks li.lv-ok .dot { background: #6b9e7f; }
.checks li.lv-warn .dot { background: #c79a48; }
.checks li.lv-bad .dot { background: #c06b5c; }
.checks .ct { flex: 1 1 auto; }
.checks .ct b { font-weight: 500; color: #222; }
.checks .ct em { display: block; font-style: normal; font-size: 11px; color: #b0b0aa; margin-top: 3px; letter-spacing: .03em; }
