# Composite Endpoint Rules

Only use a composite endpoint when the components are clinically coherent and the combination improves the study logic rather than merely increasing event count.

State explicitly:
- each component
- first-event or hierarchical rule
- whether severity differs substantially across components
- why the composite is better than separate reporting
